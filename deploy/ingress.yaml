apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: movie-app-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: movie-app-service
            port:
              number: 5000
