apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: movie-app-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: movie-app-service
            port:
              number: 80
